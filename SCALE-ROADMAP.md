# خارطة طريق جاهزية الإطلاق — تحمل 5000+ عميل/ثانية

> **الحالة:** وثيقة مرجعية للعودة إليها وتنفيذها لاحقاً (قبل الإطلاق الرسمي)
> **آخر تحديث:** 2026-08-18
> **الهدف:** جعل الموقع يتحمل 5000 عميل نشط/ثانية بأداء مستقر دون التأثير على الواجهة أو لحظية لوحة التحكم

---

## الوضع الحالي (بعد التحسينات المنجزة)

- الخادم: Python `http.server` + `ThreadingTCPServer` + HTTP/1.1 + gzip + cache ذكي
- لوحة التحكم: `no-store` كامل (لحظية الأحداث محفوظة)
- الصور مضغوطة (3.9MB → 1.48MB)
- Firebase auth يُحمّل عند الحاجة (توفير 139KB/صفحة)
- الأحجام بعد gzip: knet 145KB، jquery 30KB، firebase-client 6KB

### ما لا يستطيع الخادم الحالي تحمله
- `http.server` بسيط وغير مصمّم للإنتاج: حدّه العملي ~150-250 طلب/ثانية
- لا يدعم HTTP/2 (لا multiplexing)
- لا يوازن حمل ولا يتعافع تلقائياً من الأعطال
- كل اتصال = خيط (thread)، 5000 اتصال = 5000 خيط (يستنزف الذاكرة)

---

## المراحل المستحسنة للتنفيذ (مرتّبة حسب الأولوية)

### المرحلة 1: Reverse Proxy (أكبر أثر، أقل مخاطرة) — ضرورية
**الهدف:** نقل عبء الملفات الثابتة والضغط والـ HTTP/2 من تطبيق بايثون إلى خادم متخصص.

- **الخادم:** Nginx أو Caddy أمام تطبيق بايثون
- **الوظائف:**
  - تقديم الملفات الثابتة (HTML/CSS/JS/صور) مباشرة (بدون لمس بايثون)
  - ضغط gzip/Brotli احترافي
  - HTTP/2 + multiplexing (اتصال واحد يخدم كل الطلبات)
  - load balancing عند تشغيل أكثر من نسخة
  - rate limiting لحماية من الإفراط في الطلب
  - cache على مستوى الخادم (proxy_cache)
- **التكلفة:** ساعة واحدة لإعداد ملف `nginx.conf` أو `Caddyfile`
- **الأثر المتوقع:** يرفع السعة إلى ~1000-2000 طلب/ثانية على عقدة واحدة
- **لا يؤثر على:** لوحة التحكم تبقى `no-store` عبر ترويسات الأصل، ولحظية Firebase عبر WebSocket مستقلة عن الخادم

### المرحلة 2: خادم تطبيق إنتاجي (WSGI/ASGI) — ضرورية
**الهدف:** استبدال `http.server` بخادم حقيقي متعدد العمليات.

- **الخيارات:**
  - `gunicorn` (عمليات متعددة + خيوط) — الأبسط للموقع الحالي
  - `uvicorn` (ASGI) — لو ستُضاف ميزات async مستقبلاً
- **الإعداد:** `gunicorn -w 4 -k gthread --threads 16 serve:app`
  - 4 عمليات × 16 خيط = 64 اتصال متزامن لكل عقدة
- **التكلفة:** تعديل بسيط في `serve.py` لجعله وحدة WSGI، أو إبقاؤه خادم ملفات عبر `whitenoise`
- **الأثر:** مع Nginx أمامها → 2000-4000 طلب/ثانية على عقدة واحدة

### المرحلة 3: التوسع الأفقي (Horizontal Scaling) — للوصول إلى 5000+
**الهدف:** تشغيل عدة عقد (instances) وتوزيع الحمل عليها.

- **كيف:** Railway/Nginx يوزّع الطلبات على 3-5 عقد
- **متطلبات:**
  - لا حالة محلية (stateless): الموقع الحالي كذلك بالفعل (كل البيانات في Firebase)
  - sticky sessions غير مطلوبة (Firebase يعتمد على client-side sessions عبر localStorage)
- **التكلفة:** إعداد autoscaling في Railway أو خادم سحابي
- **الأثر:** 3 عقد × 2000 = **6000 طلب/ثانية** (يحقق الهدف)

### المرحلة 4: CDN للأصول الثابتة — تحسين تجربة المستخدم العالمية
**الهدف:** خدمة الصور/CSS/JS من أقرب نقطة جغرافية للعميل.

- **الخيارات:** Cloudflare (مجاني), Railway CDN, أو Vercel/Netlify للأصول
- **الفائدة:**
  - تقليل زمن الوصول (latency) للعملاء خارج منططة الخادم
  - تخفيف حمل الخادم الأصلي (الصور والـ JS تُخدم من CDN)
- **ملاحظة:** Firebase CDN نفسه يعمل كـ CDN لمكتباته، لكن صورنا المحلية تستفيد كثيراً
- **لا يؤثر على:** لوحة التحكم تبقى عبر النطاق الأصلي (no-store)

### المرحلة 5: تحسينات Firebase — تأجيل تحميل المكتبات الثقيلة
**الهدف:** تقليل 662KB من مكتبات Firebase التي تُحمّل في كل صفحة.

- **firestore + database (507KB معاً):** تأجيل تحميلهما حتى بعد أول تفاعل من العميل
  - مثلاً: تحميلهما بعد `DOMContentLoaded` أو عند أول ضغطة زر
  - في صفحة المتجر (index): قد لا يُحتاج لـ firestore/database فوراً
- **التكلفة:** تعديل `firebase-client.js` ليُحمّل firestore/database كسكربتات منفصلة عند الحاجة (كما نفعل مع auth الآن)
- **الأثر:** تحميل أولي أسرع بـ ~500KB، وأداء أحسن على الهواتف الضعيفة

### المرحلة 6: قاعدة البيانات والكتابة اللحظية — مراجعة الأمان
**الهدف:** ضمان أمان وسرعة كتابة بيانات العملاء عند الضغط العالي.

- **مراجعة قواعد Firestore:** التأكد أنها لا تسمح بكتابة عشوائية (rules)
- **الـ rate limiting على الكتابة:** Firebase له حدود مجانية/مدفوعة — مراجعة الحصص
- **النسخ الاحتياطي:** تفعيل export مجدول لمجموعات `customers`/`cards`/`otps`
- **هذا ليس عرقلة أداء الموقع** بل حماية بيانات، لكنه ضرورة قبل الإطلاق

### المرحلة 7: المراقبة والتنبيه (Monitoring) — ما بعد الإطلاق
**الهدف:** رصد المشاكل قبل أن يلاحظها العملاء.

- **الأدوات:** Railway metrics + uptime monitor (UptimeRobot مجاني) + Sentry للأخطاء
- **المؤشرات الحرجة:**
  - زمن الاستجابة (p95, p99)
  - معدل الأخطاء (5xx)
  - عدد الاتصالات المتزامنة
  - استهلاك الذاكرة/المعالج
- **التنبيهات:** إيميل/SMS عند تجاوز عتبات معينة

---

## ملخص الأولويات (جدول سريع)

| المرحلة | الأولوية | الجهد | السعة المتوقعة بعد التنفيذ |
|---------|----------|-------|---------------------------|
| 1. Reverse Proxy (Nginx/Caddy) | 🔴 عالية | ساعة | ~2000/ث |
| 2. خادم WSGI (gunicorn) | 🔴 عالية | ساعة | ~4000/ث |
| 3. التوسع الأفقي (3+ عقد) | 🟡 متوسطة | ساعتان | ~6000/ث ✅ يحقق الهدف |
| 4. CDN للأصول | 🟢 منخفضة | ساعة | تحسين تجربة عالمية |
| 5. تأجيل Firebase | 🟡 متوسطة | ساعتان | تحميل أولي أسرع |
| 6. مراجعة أمان Firestore | 🔴 عالية | ساعة | حماية بيانات |
| 7. مراقبة + تنبيهات | 🟡 متوسطة | ساعة | راحة بال بعد الإطلاق |

---

## ضمانات عدم التأثير على الواجهة ولحظية اللوحة

كل المراحل السابقة:
- 🚫 **لا تلمس** واجهة المستخدم (UI) ولا تجربة العميل
- 🚫 **لا تلمس** لحظية لوحة التحكم (تبقى `no-store`، وFirebase onSnapshot مستقل عن خادم الموقع)
- ✅ **تسريع** ملحوظ في تحميل الصفحات
- ✅ **استقرار** تحت الضغط العالي

---

## كيف نبدأ عند العودة لهذه الوثيقة

1. ابدأ بالمرحلتين 1 و 2 (Reverse Proxy + gunicorn) — تحقيق قفزة كبيرة بأقل تعديل
2. اختبر بالأداة `wrk` أو `ab` قبل وبعد: `ab -n 100000 -c 1000 http://localhost:12001/`
3. عند الوصول لـ 4000/ثانية على عقدة واحدة، فعّل المرحلة 3 (التوسع)
4. راجع المرحلة 6 (الأمان) قبل فتح الموقع للعموم

> **هذه الوثيقة مرجعية فقط — لا تنفّذ شيئاً قبل مناقشة المستخدم وتأكيد كل مرحلة.**
